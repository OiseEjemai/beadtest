// src/components/Balance.jsx
import React from 'react';

const Balance = ({ balance }) => {
    return (
        <div>
            <h2>Your Balance: {balance} Naira</h2>
        </div>
    );
};

export default Balance;
